# خطة إعادة هيكلة البوابة الرئيسية `/home`

## 🚨 الهدف
حل مشكلة الانهيار (Crash) عبر:
1.  **حذف المحتوى الثقيل** من الصفحة الرئيسية `/home` وجعلها صفحة تنقل (Menu) فقط.
2.  **نقل الأدوات التحليلية** (الرادار، لوحة الشرف) إلى صفحة جديدة باسم `/insights` (رؤى).
3.  **تغيير التوجيه بعد الدخول** ليذهب مباشرة إلى `/sessions` (الحصص اليومية) بدلاً من `/home`.

## 🛠️ الخطوات التنفيذية

### 1. تعديل التوجيه (Redirection)
*   تعديل ملف `src/app/login/page.tsx`:
    *   تغيير `router.push('/home')` إلى `router.push('/sessions')`.

### 2. إنشاء صفحة "رؤى" (`/insights`)
*   إنشاء ملف جديد `src/app/insights/page.tsx`.
*   نقل المكونات التالية إليه:
    *   `GroupEvaluationCard` (الرادار/المنحنى)
    *   `RecentMilestones` (شريط الإنجازات)
    *   `HallOfFame` (لوحة الشرف)
    *   `ImpactStats` (الإحصائيات السريعة)
*   إضافة `GlobalErrorBoundary` لحماية هذه الصفحة أيضاً.

### 3. تنظيف الصفحة الرئيسية (`/home`)
*   حذف جميع المكونات أعلاه من `src/app/home/page.tsx`.
*   الاحتفاظ فقط بـ:
    *   شريط العنوان (Title Bar)
    *   قائمة التنقل (Navigation Grid)
    *   التذييل (Footer)

### 4. تحديث القوائم
*   إضافة رابط لصفحة `/insights` في قائمة التنقل (navItems) في `src/app/home/page.tsx`.
*   تحديث ملف الصلاحيات `src/lib/permissions.ts` للسماح بالوصول لـ `/insights`.

---
**ملاحظة:** هذا الحل سيضمن أن المستخدم يدخل فوراً لصفحة الحصص (التي تعمل جيداً)، والصفحة الرئيسية ستكون خفيفة جداً ولن تنهار.
