# خطوات رفع التعديلات إلى GitHub

اتبع الخطوات التالية لرفع التعديلات الحالية إلى مستودع المشروع على GitHub:

## 1. تهيئة Git (إذا لم تكن مهيأة سابقاً)
افتح التيرمينال في مجلد المشروع وقم بتشغيل الأمر التالي:
```bash
git init
```

## 2. إضافة رابط المستودع (Remote)
قم بتشغيل هذا الأمر لربط المجلد المحلي بمستودع GitHub:
```bash
git remote add origin https://github.com/adaikamohammed/Al-Shafi-i-Group.git
```
*ملاحظة: إذا كان الرابط موجوداً مسبقاً، يمكنك استخدامه مباشرة.*

## 3. إضافة التعديلات
لإضافة كافة الملفات المعدلة:
```bash
git add .
```

## 4. تسجيل التعديلات (Commit)
قم بكتابة رسالة توضح التغييرات التي أجريتها:
```bash
git commit -m "تحديث التعديلات الحالية"
```

## 5. رفع الملفات إلى GitHub (Push)
لرفع الملفات إلى الفرع الرئيسي (main):
```bash
git push -u origin main
```
*إذا كان اسم الفرع (master)، استبدل main بـ master.*

---
**ملاحظة:** إذا واجهت مشكلة في الصلاحيات، قد يطلب منك Git تسجيل الدخول إلى حسابك في GitHub.
