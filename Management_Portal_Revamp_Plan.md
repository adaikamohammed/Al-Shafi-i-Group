# خطة تطوير بوابة الإدارة (Management Portal Revamp)

## الهدف
تحسين تجربة حساب الإدارة (`admin00`) ليكون "لوحة تحكم استراتيجية" بدلاً من مجرد حساب مستخدم عادي بصلاحيات أعلى. التركيز على **المقارنة بين الأفواج**، **الإحصائيات المالية**، و **إخفاء الأدوات اليومية** غير الضرورية للإدارة العليا.

## 1. تعديل الصلاحيات والقوائم (Permissions & Navigation)

سنقوم بتنقيح القائمة الجانبية لحساب `management` لإخفاء الصفحات التشغيلية البحتة وإبراز الصفحات الإدارية.

### الصفحات المقترح إخفاؤها عن الإدارة:
*   ❌ **الحصص اليومية (`/sessions`)**: الإدارة تراقب التقارير ولا تسجل الحضور بنفسها يومياً.
*   ❌ **تقرير الطالب (`/reports/student`)**: الإدارة تهتم بالتقارير العامة، تقرير الطالب الفردي متاح عبر البحث في "إدارة الطلبة" عند الحاجة، وليس كبند رئيسي.
*   ❌ **دليل الاستخدام (`/guide`)**: غير ضروري للإدارة العليا.

### الصفحات المقترح إبقاؤها/تحسينها:
*   ✅ **الرئيسية (`/`)**: تم تحسينها بالفعل (Dashboard).
*   ✅ **المستحقات المالية (`/dues`)**: سيتم تحويلها لـ "لوحة مالية".
*   ✅ **ساحة المقارنة (`/comparison`)**: سيتم تعزيزها.
*   ✅ **إدارة الطلبة (`/data`)**: البقاء عليها للوصول لقاعدة البيانات كاملة.

## 2. تطوير صفحة المستحقات المالية (`/dues`)
حالياً: تعرض جدولاً بأسماء الطلاب ودفعاتهم.
التعديل المطلوب: إضافة "لوحة قيادة مالية" في أعلى الصفحة تظهر **فقط** لحساب الإدارة.

### الميزات الجديدة:
1.  **مخطط بياني للمقارنة (Bar Chart)**:
    *   محور X: أسماء الأفواج (فوج 1، فوج 2...).
    *   محور Y: نسبة السداد (%) أو إجمالي المبلغ المحصل.
    *   أشرطة ملونة: (مدفوع ✅، غير مدفوع ❌، معفى ⚠️).

2.  **بطاقات ملخص (Summary Cards)**:
    *   إجمالي المداخيل المتوقعة vs المحصلة.
    *   الفوج "الأكثر التزاماً" بالسداد.

## 3. تعميم مبدأ "المقارنة"
سنقوم بإضافة "تبويب مقارنة" في الصفحات الرئيسية الأخرى:

*   **صفحة الحفظ (`/surahs`)**: إضافة رسم بياني يقارن "متوسط الحفظ" لكل فوج.
*   **صفحة النقاط (`/points`)**: مقارنة "مجموع النقاط" بين الأفواج.

## خطوات التنفيذ (Implementation Steps)

### المرحلة 1: تحديث الصلاحيات
1.  تعديل `src/lib/permissions.ts` لإزالة `management` من الصفحات غير الضرورية.
2.  تحديث `src/app/home/page.tsx` للتأكد من أن القائمة تعكس هذه التغييرات.

### المرحلة 2: تحديث صفحة المستحقات (`src/app/dues/page.tsx`)
1.  إنشاء مكون جديد `RevenueComparisonChart.tsx`.
2.  دمجه في صفحة المستحقات بشرط `if (isManagement)`.
3.  حساب البيانات (تجميع الدفعات حسب `student.groupName`).

### المرحلة 3: (اختياري) تحسينات إضافية
1.  إضافة مكونات مقارنة مشابهة في الصفحات الأخرى إذا طلب المستخدم ذلك لاحقاً.

---
**ملاحظة**: سنبدأ فوراً بتنفيذ المرحلة 1 و 2.
