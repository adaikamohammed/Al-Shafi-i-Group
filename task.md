- [ ] وضع السمة الافتراضية الفاتحة (Default Light Mode) <!-- id: 10 -->
    - [ ] تعديل `ClientLayout.tsx` ليكون الافتراضي هو `classic`
    - [ ] تعديل `useTheme.ts` ليكون الافتراضي هو `light` و `classic`
- [ ] إعداد وتكامل نظام إشعارات الـ Push Notifications <!-- id: 11 -->
    - [ ] إنشاء مكون `NotificationRegister.tsx` لطلب الإذن وجلب الـ FCM Token وحفظه في Firebase
    - [ ] ربط المكون في `ClientLayout.tsx` ليعمل لجميع المستخدمين المسجلين
    - [ ] إنشاء مسار الـ API لإرسال الإشعارات `src/app/api/notify/route.ts` باستخدام FCM HTTP v1
    - [ ] ربط منطق التحقق التلقائي (3 غيابات أو 3 مرات عدم حفظ) في `StudentContext.tsx`
    - [ ] إضافة واجهة إدارة وتفعيل الإشعارات وزر الاختبار في صفحة الإعدادات `settings/page.tsx`
