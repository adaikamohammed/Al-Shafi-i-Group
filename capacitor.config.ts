import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alshafii.quran',
  appName: 'AlShafii Quran App',
  webDir: 'public'
};

export default config;
