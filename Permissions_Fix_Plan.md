# خطة معالجة صلاحيات الإدارة (Management Permissions Fix Plan)

## 🚨 المشكلة الحالية
رغم وجود زر "تهيئة البيانات" وإصلاح الكود، لا تزال لوحة التحكم فارغة (0 طلبة).
هذا يؤكد بنسبة 99% أن **قواعد الأمان (Security Rules) في Firebase تمنع قراءة البيانات**، حتى لو كانت البيانات موجودة.

السبب: القواعد السابقة ربما كانت معقدة جداً أو تحتوي على شرط غير متحقق (مثل قراءة `role` من مكان غير صحيح).

## 🛠️ الحل الجذري: تبسيط القواعد (Rule Simplification)
سنقوم بتحديث ملف `database.rules.json` بقواعد **أبسط وأكثر مباشرة** لضمان عمل حساب الإدارة.

### ما يجب عليك فعله يدوياً في Firebase Console:
بما أنني لا أملك وصولاً مباشراً لتغيير الإعدادات في لوحة تحكم Firebase الخاصة بك، **يجب عليك أنت القيام بهذه الخطوة يدوياً**.

1.  افتح [Firebase Console](https://console.firebase.google.com/).
2.  انتقل إلى **Realtime Database** > **Rules**.
3.  **احذف كل شيء** واستبدله بالكود الجديد الذي سأعطيك إياه (موجود في ملف `database.rules.json`).
4.  اضغط **Publish**.

### القواعد الجديدة المبسطة
سنسمح لأي مستخدم مسجل الدخول (`auth != null`) بالقراءة والكتابة مؤقتاً للتأكد من أن المشكلة ليست في الكود، أو سنستخدم قواعد مخصصة ولكن أقل تعقيداً.

**الخيار الأفضل (الموازن بين الأمان والتشغيل):**
سنعتمد على `root.child('users').child(auth.uid)` للتحقق من الدور.

---

## 📝 الخطوات القادمة
1.  سأقوم بتحديث ملف `database.rules.json` الآن بقواعد مضمونة.
2.  عليك نسخ محتواه وتطبيقه في الكونسول.
3.  بعد ذلك، جرب الضغط على زر "تهيئة النظام" مرة أخرى.
