# دليل نظام سجل النشاطات (Activity Log System)

تم الانتهاء من تنفيذ نظام سجل النشاطات الذي يسمح للمشرف العام (`admin00`) بمراقبة كافة التحديثات في النظام.

## التغييرات التي تم إجراؤها

### 1. قاعدة البيانات (`database.rules.json`)
- تم إضافة عقدة `activity_logs` في قاعدة بيانات Firebase.
- تم تعيين صلاحيات القراءة للإدارة  والكتابة للمشايخ والإدارة.

### 2. واجهة المستخدم (`src/app/management/logs/page.tsx`)
- صفحة جديدة تعرض جدولاً زمنياً للنشاطات.
- **المميزات:**
  - تصفية حسب نوع العملية (إضافة، تعديل، حذف).
  - تصفية حسب الشيخ (القائم بالعملية).
  - بحث سريع.
  - إحصائيات يومية.

### 3. نظام التسجيل (`src/lib/activityLogger.ts` & `StudentContext.tsx`)
- تم إنشاء أداة تسجيل مركزية `logActivity`.
- تم دمج التسجيل تلقائياً في العمليات التالية:
  - إضافة/تعديل/حذف طالب.
  - تسجيل/حذف حصة يومية.
  - حفظ/حذف تقرير يومي.

### 4. الوصول السريع (`ManagementDashboard.tsx`)
- تم إضافة زر "سجل النشاطات" في لوحة تحكم الإدارة لسهولة الوصول.

## كيفية التحقق (Verification)

1. **الدخول كمسؤول (`admin00`)**:
   - انتقل إلى لوحة القيادة.
   - اضغط على الزر الجديد **"سجل النشاطات"**.
   - تأكد من ظهور الصفحة فارغة (أو بها بيانات قديمة إذا وجدت).

2. **تجربة التسجيل**:
   - قم بتسجيل الدخول بحساب شيخ (أو استخدم المشرف لإضافة طالب).
   - قم بإضافة طالب جديد أو تسجيل حصة.
   - عد إلى صفحة السجلات وتأكد من ظهور النشاط فوراً (Real-time).

3. **الفلاتر**:
   - جرب البحث عن اسم الطالب الذي أضيفته.
   - جرب فلترة العمليات لعرض "إضافة طالب" فقط.

## ملاحظات
- السجلات تحفظ في Firebase تحت المسار `/activity_logs`.
- يتم عرض آخر 200 نشاط لتحسين الأداء.
